[project]
name = "ags-device"
version = "0.1.0"
description = "Autonomous Gardening System Device Backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["paho-mqtt>=2.1.0","falcon>=4.0.2"]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.2",
    "pytest-cov>=4.1.0",
    "pytest-mock>=3.10.0",
    "ruff>=0.11.8",
    "uvicorn>=0.34.2"
]
